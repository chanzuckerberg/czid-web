<!--Show a "mobile-friendly" page -->
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<div id="landing_page">
  <%= javascript_tag do %>
    react_component(
      'DiscoveryViewRouter', {
        announcementBannerEnabled: <%= @announcement_banner_enabled %>,
      }, 'landing_page', JSON.parse('<%= raw escape_json(user_context)%>')
    );
  <% end %>
</div>
