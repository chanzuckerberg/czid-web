<%= react_component 'SampleUpload', {projects: @projects, csrf: form_authenticity_token, host_genomes: @host_genomes}%>

<div style="margin:15px;">
<h1>New Sample</h1>
<%= render 'form', sample: @sample %>

<%= link_to 'See all samples', samples_path %> |
<%= link_to 'Front portal', root_path %>
</div>