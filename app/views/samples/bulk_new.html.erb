<div id="bulk_page">
  <%= javascript_tag do %>
    react_component('BulkUploadImport', {
      projects: JSON.parse('<%= raw @projects.to_json %>'),
      csrf: '<%= form_authenticity_token %>',
      host_genomes: JSON.parse('<%= raw @host_genomes.to_json %>'),
      loggedin_user: JSON.parse('<%= raw current_user.to_json %>')
    }, 'bulk_page');
  <% end %>
</div>
