
<%= javascript_tag do %>
  react_component(
    'AMRHeatmapView',
    { sampleIds: JSON.parse('<%= raw escape_json(@sample_ids) %>') },
    'page_content',
    JSON.parse('<%= raw escape_json(user_context)%>')
  )
<% end %>
