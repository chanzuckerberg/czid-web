<%= form_with(model: pipeline_output, local: true) do |form| %>
  <% if pipeline_output.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pipeline_output.errors.count, "error") %> prohibited this pipeline_output from being saved:</h2>

      <ul>
      <% pipeline_output.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sample_name %>
    <%= form.text_field :sample_name, id: :pipeline_output_sample_name %>
  </div>

  <div class="field">
    <%= form.label :pipeline_output %>
    <%= form.file_field :pipeline_output, id: :pipeline_output %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
