import React from "react";

const InsightMessage = ({ content }) => {
  return (
    <div className="ui message white idseq-ui insight-message">{content}</div>
  );
};

export default InsightMessage;
