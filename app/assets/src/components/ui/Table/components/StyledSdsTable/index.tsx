import { StyledSdsTable } from "./StyledSdsTable";
export { StyledSdsTable };

/*
 NOTE: component code should go in StyledSdsTable.tsx
 This index file is here so our imports are cleaner.
 This way we can use
 import { StyledSdsTable } from .../StyledSdsTable
 rather than
 import { StyledSdsTable } from .../StyledSdsTable/StyledSdsTable.tsx
*/
