import { SortableHeader } from "./SortableHeader";
export { SortableHeader };

/*
 NOTE: component code should go in SortableHeader.tsx
 This index file is here so our imports are cleaner.
 This way we can use
 import { SortableHeader } from .../SortableHeader
 rather than
 import { SortableHeader } from .../SortableHeader/SortableHeader.tsx
*/