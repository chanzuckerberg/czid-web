import { MetadataValue } from "./MetadataValue";
export { MetadataValue };

/*
 NOTE: component code should go in MetadataValue.tsx
 This index file is here so our imports are cleaner.
 This way we can use
 import { MetadataValue } from .../MetadataValue
 rather than
 import { MetadataValue } from .../MetadataValue/MetadataValue.tsx
*/
