import { SampleMessage } from "./SampleMessage";
export { SampleMessage };

/*
 NOTE: component code should go in SampleMessage.tsx
 This index file is here so our imports are cleaner.
 This way we can use
 import { SampleMessage } from .../SampleMessage
 rather than
 import { SampleMessage } from .../SampleMessage/SampleMessage.tsx
*/
