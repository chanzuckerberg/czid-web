import { StackedSampleIds } from "./StackedSampleIds";
export { StackedSampleIds };

/*
 NOTE: component code should go in StackedSampleIds.tsx
 This index file is here so our imports are cleaner.
 This way we can use
 import { StackedSampleIds } from .../StackedSampleIds
 rather than
 import { StackedSampleIds } from .../StackedSampleIds/StackedSampleIds.tsx
*/
