import { ReportViewSelector } from "./ReportViewSelector";
export { ReportViewSelector };

/*
 NOTE: component code should go in ReportViewSelector.tsx
 This index file is here so our imports are cleaner.
 This way we can use
 import { ReportViewSelector } from .../ReportViewSelector
 rather than
 import { ReportViewSelector } from .../ReportViewSelector/ReportViewSelector.tsx
*/
