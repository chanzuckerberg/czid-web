import { HoverActions } from "./HoverActions";
export { HoverActions };

/*
 NOTE: component code should go in HoverActions.tsx
 This index file is here so our imports are cleaner.
 This way we can use
 import { HoverActions } from .../HoverActions
 rather than
 import { HoverActions } from .../HoverActions/ReportPanel.tsx
*/
